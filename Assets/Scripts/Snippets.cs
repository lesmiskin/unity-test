// ===============================
// MOVE OBJECT IN A CIRCLE PATTERN
// ===============================
/*
// Hold these variables outside the method scope:
public float movementIncX = 0;
public float movementIncY = 0;

var newx = MqCommon.sineInc(0, ref movementIncX, 0.04f, 100f);
var newy = MqCommon.cosInc(0, ref movementIncY, 0.04f, 100f);

Vector3 movement = new Vector3(newx, newy, 0);
movement *= Time.deltaTime;

transform.position = movement;
*/
